using UnityEngine;

public class gameManagerTD : MonoBehaviour
{
    // Start is called once before the first execution of Update after the MonoBehaviour is created
    void Start()
    {
        // Switch to 640 x 480 full-screen
        Screen.SetResolution(640, 1080, true);
    }
}

